# 标准管理系统 - 后端接口详细文档 (v1.0)

本文档旨在详尽描述标准管理系统后端的 API 接口，包括请求路径、请求方法、请求报文示例及响应报文示例。

## 1. 基础信息

*   **Base URL**: `/api/`
*   **数据格式**: Content-Type: `application/json`
*   **状态码说明**:
    *   `200`: 请求成功
    *   `400`: 请求参数错误
    *   `404`: 资源不存在
    *   `500`: 服务器内部错误

---

## 2. 标准管控核心接口 (StdBaseViewSet)

本模块基于 Django REST Framework 的 `ModelViewSet`，前缀为 `/api/standards/`。

### 2.1 列表/搜索标准
*   **URL**: `GET /api/standards/`
*   **Query 参数**: 
    *   `bz_id` (可选): 匹配标准号
    *   `bz_name` (可选): 匹配标准名称
    *   `ex_state` (可选): 执行状态 (如：现行, 废止)
    *   `search` (可选): 全局搜索关键字
*   **响应示例**:
    ```json
    {
        "count": 100,
        "next": "...",
        "previous": null,
        "results": [
            {
                "id": 1,
                "bz_id": "GB/T 1.1-2020",
                "bz_name": "标准化工作导则 第1部分：标准化文件的结构和起草规则",
                "bz_release_date": "2020-03-31",
                "implement_time": "2020-10-01",
                "ex_state": "现行",
                "replace_bz_id": "GB/T 1.1-2009",
                "tree_id": "T001"
            }
        ]
    }
    ```

### 2.2 查询最新演进状态
*   **URL**: `GET /api/standards/check-latest/`
*   **Query 参数**: `bz_id=GB/T 1.1-2009`
*   **请求说明**: 溯源链条追查，返回最新现行版。
*   **响应示例**:
    ```json
    {
        "query_bz_id": "GB/T 1.1-2009",
        "is_latest": false,
        "current_latest_id": "GB/T 1.1-2020",
        "pedigree_chain": "{GB/T 1.1-2020}、{GB/T 1.1-2009}、{GB/T 1.1-2000}"
    }
    ```

### 2.3 导出合规性报表 (Excel)
*   **URL**: `POST /api/standards/export-report/`
*   **请求报文**:
    ```json
    {
        "bz_ids": ["GB/T 1.1-2020", "GB/T 20000.1-2014"]
    }
    ```
*   **响应**: 返回文件流 (`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`)。

### 2.4 基础模糊搜索 (工作台)
*   **URL**: `GET /api/standards/basic-search/`
*   **Query 参数**: `q=1.1`
*   **响应示例**:
    ```json
    {
        "code": 200,
        "msg": "success",
        "data": [
            {
                "id": 1,
                "bz_id": "GB/T 1.1-2020",
                "bz_name": "...",
                "ex_state": "现行",
                "release_date": "2020-03-31",
                "implement_time": "2020-10-01"
            }
        ]
    }
    ```

### 2.5 批量模糊查询 (族谱逆推)
*   **URL**: `POST /api/standards/batch-fuzzy-check/`
*   **请求报文**:
    ```json
    {
        "keywords": ["GB/T 1.1", "GB/T 20000.1"]
    }
    ```
*   **响应示例**:
    ```json
    {
        "code": 200,
        "msg": "查询成功",
        "data": [
            {
                "id": "GB/T 1.1-2020",
                "name": "...",
                "isLatest": true,
                "latestId": "GB/T 1.1-2020",
                "error": null
            }
        ]
    }
    ```

### 2.6 级联依赖反向预警
*   **URL**: `GET /api/standards/warning-trace/`
*   **Query 参数**: `bz_id=GB/T 1.1-2009`
*   **请求说明**: 分析国标被废止后，牵连的企标有哪些。
*   **响应示例**:
    ```json
    {
        "code": 200,
        "msg": "success",
        "data": {
            "is_safe": false,
            "input_bz": "GB/T 1.1-2009",
            "latest_bz": "GB/T 1.1-2020",
            "affected_enterprises": ["Q/ABC 001-2023", "Q/XYZ 123-2022"]
        }
    }
    ```

### 2.7 大屏统计数据
*   **URL**: `GET /api/standards/statistics/`
*   **响应示例**:
    ```json
    {
        "code": 200,
        "msg": "success",
        "data": {
            "types": { "GB": 1500, "QB": 300, "HB": 50, "TB": 20, "DB": 80 },
            "states": { "现行": 1200, "废止": 600, "即将实施": 50 }
        }
    }
    ```

### 2.8 PDF 文件下载
*   **URL**: `GET /api/standards/download-doc/`
*   **Query 参数**: `bz_id=GB/T 1.1-2020`
*   **响应**: 返回 PDF 文件流。若文件不存在返回 404。

---

## 3. Dify AI 深度集成接口 (views_dify.py)

### 3.1 企标规范性引用合规性评价 (AI 驱动)
*   **URL**: `POST /api/check_references/`
*   **请求报文**:
    ```json
    {
        "qibiao_release_date": "2023-01-01",
        "references": [
            { "standard_id": "GB/T 1.1", "has_year": false, "full_text": "GB/T 1.1 标准化工作导则" }
        ]
    }
    ```
*   **响应示例**:
    ```json
    {
        "success": true,
        "evaluation_results": [
            {
                "original_text": "GB/T 1.1 标准化工作导则",
                "status": "有效",
                "message": "该 GB/T 1.1-2020 能够继续使用。"
            }
        ]
    }
    ```

### 3.2 提取并插入标准指标 (AI 提取)
*   **URL**: `POST /api/insert_indexes/`
*   **请求报文**: (由 Dify 吐出的 JSON 后端解析)
    ```json
    {
        "bz_id": "GB/T 12345-2023",
        "indexes": [
            { "index_name": "抗拉强度", "index_type": "具体值", "index_context": ">= 300MPa" }
        ]
    }
    ```
*   **响应示例**:
    ```json
    {
        "success": true,
        "message": "成功！标准 [GB/T 12345-2023] 的 1 个指标已入库 (status=0)！"
    }
    ```

### 3.3 审核员裁决结果提交
*   **URL**: `POST /api/audit/submit/`
*   **请求报文**:
    ```json
    {
        "id": 101,
        "action": "approve",
        "modified_content": { "提取内容": ">= 350MPa" },
        "modified_bz_id": "GB/T 12345.1-2023",
        "modified_index_name": "核心抗拉强度"
    }
    ```
*   **响应示例**: 
    ```json
    { "success": true, "message": "ID:101 审核操作成功！" }
    ```

### 3.4 异步文件解析触发 (Celery)
*   **URL**: `POST /api/analyze_qb_references_auto/`
*   **请求**: `multipart/form-data` 携带 `file` 字段。
*   **响应示例**:
    ```json
    {
        "code": 200,
        "msg": "✅ 文件已成功移交后台 AI 引擎！..."
    }
    ```

---

## 4. 查重与语义分析 (views_duplicate.py)

### 4.1 字面查重 (一审)
*   **URL**: `POST /api/duplicate/name-check/`
*   **请求报文**: `{"keyword": "电池回收"}`
*   **响应示例**:
    ```json
    {
        "success": true,
        "message": "成功匹配到 5 条相关标准",
        "data": [
            { "bz_id": "GB/T 123", "bz_name": "电池回收规程", "status": "现行", "similarity": 85.0 }
        ]
    }
    ```

### 4.2 语义高维查重分析 (二审 - 异步)
*   **URL**: `POST /api/duplicate/semantic-check/`
*   **请求报文**:
    ```json
    {
        "intent_text": "我准备立项一个关于电动车废旧电池物理拆解的技术规范...",
        "candidate_ids": ["GB/T 123", "GB/T 456"]
    }
    ```
*   **响应示例**:
    ```json
    {
        "success": true,
        "task_id": "sem_abcdef12",
        "message": "🚀 立项意图已接收！正在定向进行高维空间语义碰撞..."
    }
    ```

---

## 5. 实时预警功能 (views.py)

### 5.1 获取未读预警列表
*   **URL**: `GET /api/warnings/list`
*   **响应示例**:
    ```json
    {
        "success": true,
        "unread_count": 3,
        "data": [
            {
                "id": 1,
                "old_bz_id": "GB/T 1-2009",
                "new_bz_id": "GB/T 1-2020",
                "quote_bz": "Q/XYZ 001",
                "is_read": false,
                "create_time": "2024-03-20T10:00:00Z"
            }
        ]
    }
    ```

### 5.2 标记预警为已读
*   **URL**: `POST /api/warnings/mark_read`
*   **请求报文**: `{"warning_id": 1}`
*   **响应示例**: `{"success": true}`

---

## 6. WebSocket 实时通知

*   **通道名**: `dify_notifications`
*   **连接 URL**: `ws://<server_host>/ws/dify_notifications/`
*   **消息格式 (Action)**:
    *   **类型 1**: `preface_highlights` (前言修订差异高亮)
    *   **类型 2**: `semantic_check` (语义查重报告)
    *   **类型 3**: `send_dify_result` (通用比对结果)

---

## 7. 自动任务 (Celery Tasks)

*   `process_qb_dify_task`: AI 提取企标引用并入库映射。
*   `extract_preface_diff_task`: 从 Word/PDF 提取前言修订项差异。
*   `run_semantic_check_task`: 立项申请的语义相似度计算与 AI 裁决。

---

**附录: 常见字段说明**
- `ex_state`: 执行状态 (现行 / 即将实施 / 废止)
- `bz_id`: 标准号
- `bz_name`: 标准名称
- `tree_id`: 演进溯源家族树标识
- `index_type`: 指标类型 (具体值 / 引用值 / 其他)
