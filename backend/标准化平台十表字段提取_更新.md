# 标准化平台字段提取（更新版）

- 来源文件: `标准化平台十表（中文命名）.xlsx`
- 生成时间: 2026-04-27 11:11:27
- 工作表数量: 12；另含 **合规评价任务表** 1 张（共 **13** 张业务表口径）
- **MySQL 8.0 落地**：请以仓库内 `standardization_platform_schema_mysql80.sql` 为建表与字段类型的**执行源**；本 Markdown 为中文说明与逻辑类型对照，与 SQL 不一致时以 **`.sql` 为准**。

## 1. 企标基础信息表
- 字段数量: 5（与 `standardization_platform_schema_mysql80.sql` 一致）

| 字段名 | MySQL 8（落地） | 类型建议（逻辑） | 说明 |
|---|---|---|---|
| id | BIGINT NOT NULL AUTO_INCREMENT | bigserial | 主键 |
| 企标号(qb_code) | VARCHAR(128) NOT NULL，唯一 | varchar(128) | 与映射表外键类型一致 |
| 企业名称(company_name) | TEXT | text | 企业名称 |
| 企标名称(qb_name) | TEXT | text | 企标名称 |
| 指标集合(indicator_set_json) | JSON | jsonb | 企标具体指标集合 |

## 2. 国标基础信息扩展表
- 字段数量: 10

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| 国标号(std_code) | varchar(128) | 标准号原文 |
| 归口单位/部门(responsible_unit) | text |  |
| 副归口单位(secondary_responsible_unit) | text |  |
| 颁发部门(issuing_department) | text |  |
| 执行单位(executing_unit) | text |  |
| 技术委员会(technical_committee) | text |  |
| 主管部门(governing_department) | text |  |
| 采标情况(adoption_status) | text |  |
| 起草单位(drafting_unit) | text |  |
| 起草人(drafter) | text |  |

## 3. 国标基础信息主表
- 字段数量: 16

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 国标号(std_code) | varchar(128) | 标准号原文 |
| 标准名称(std_name) | text | 标准名称 |
| 标准状态(std_status) | enum_std_status | draft/active/upcoming/abolished |
| 发布日期(publish_date) | date | 发布日期 |
| 实施日期(effective_date) | date | 实施日期 |
| 废止日期(abolition_date) | date |  |
| 标准类型(std_type) | varchar(64) |  |
| 标准类别(std_category) | varchar(64) |  |
| 代替标准(replaces_std_code) | varchar(128) | 被本标准替代的旧标准号，最长1023 |
| 代替类型(replace_type) | varchar(64) | 代替关系类型：-1无；0未知；1完全替代；2部分替代 |
| 中国标准分类号(ccs_code) | varchar(64) |  |
| 国际标准分类号(ics_code) | varchar(64) |  |
| 谱系号(ped_id) | varchar(128) |  |
| 详情链接(detail_url) | text |  |
| 国标文件保存路径(std_file_path) | text |  |

## 4. 谱系表
- 字段数量: 5

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 国标号(std_code) | varchar(128) | 标准号 |
| 国标最新版本号(latest_std_code) | varchar(128) | 新标准号 |
| 谱系号(ped_id) | varchar(128) |  |
| 谱系链(part_chain) | text |  |

## 5. 企标映射表
- 字段数量: 6

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigint | 主键 |
| 企标号(qb_code) | varchar(50) | 关联企标基础信息表 |
| 引用国标号(referenced_std_code) | varchar(50) | 企标正文引用标准号 |
| 国标最新版本号(latest_std_code) | varchar(50) | 追溯到最新国标号 |
| 补充国标版本号(supplement_std_version_code) | varchar(50) |  |
| 人工审核状态(manual_review_status) | tinyint(1) |  |

## 6. 国标映射企标表
- 字段数量: 3

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 国标号(std_code) | varchar(128) | 关联国标基础信息表 |
| 企标号(qb_code) | varchar(128) | 引用该国标的企标编号 |

## 7. 国标具体指标表
- 字段数量: 4

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 国标号(std_code) | varchar(128) | 关联国标基础信息表 |
| 国标具体指标值(specific_indicator_value) | varchar(255) | 具体指标 |
| 人工审核状态(manual_review_status) | enum_manual_review_status |  |

## 8. 评价结果表
- 字段数量: 11（与 `standardization_platform_schema_mysql80.sql` 一致）
- 说明：`compliance_task_id` 将每条评价结果与 **一次合规评价向导（任务）** 绑定，支持同一企标号多轮评价、与六步五审状态机对齐。

| 字段名 | MySQL 8（落地） | 类型建议（逻辑） | 说明 |
|---|---|---|---|
| id | BIGINT NOT NULL AUTO_INCREMENT | bigserial | 主键 |
| 企标号(qb_code) | VARCHAR(128) NOT NULL，FK | varchar(128) | 关联 `enterprise_standard_basic` |
| 合规评价任务ID(compliance_task_id) | BIGINT NOT NULL，UNIQUE，FK → `compliance_evaluation_task.id` | bigint | **一次评价任务一行结论**；与任务一对一 |
| 描述性结论(descriptive_result) | ENUM('compliant','non_compliant','partial','unknown','not_applicable') | enum_overall_result | 描述性合规结论（占位枚举） |
| 描述性结论报告路径(descriptive_result_report_file_path) | TEXT | text |  |
| 引用结论(reference_result) | ENUM(同上) | enum_overall_result | 引用合规结论 |
| 引用结论报告路径(reference_result_report_file_path) | TEXT | text |  |
| 指标结论(indicator_result) | ENUM(同上) | enum_overall_result | 指标合规结论 |
| 指标结论报告路径(indicator_result_report_file_path) | TEXT | text |  |
| 总结论(overall_result) | ENUM(同上) | enum_overall_result | 总体结论 |
| 证书路径(certificate_file_path) | TEXT | text | 证书文件路径 |

## 9. 正文目录表
- 字段数量: 7

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 标准名称(std_name) | text | 标准名称 |
| 国标号(std_code) | varchar(128) | 关联国标基础信息表 |
| 关键词(core_keywords) | text | 大模型提取关键词 |
| 正文(content_text) | text | 正文内容 |
| 语义向量(scope_vector) | json|
| 向量模型名(embedding_model) | varchar(128) | 记录生成scope_vector的模型名称/版本 |

## 10. 统计表
- 字段数量: 3

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 国标号(std_code) | varchar(128) |  |
| 引用次数(reference_count) | integer | 被企标引用次数 |

## 11. ICS行业分类表
- 字段数量: 5

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 国际标准分类号(ics_code) | varchar(64) |  |
| 层级(ics_level) | smallint |  |
| 名称(ics_name) | text |  |
| 注释(ics_note) | text |  |

## 12. CCS行业分类表 
- 字段数量: 5

| 字段名 | 类型建议 | 说明 |
|---|---|---|
| id | bigserial | 主键 |
| 名称(ccs_name) | text |  |
| 中国标准分类号(ccs_code) | varchar(64) |  |
| 父代码(parent_code) | varchar(64) |  |
| 备注(ccs_note) | text |  |

## 13. 合规评价任务表（合规模块）
- 字段数量: 2（最小可建表集；`current_step`、Dify run、快照等由后续 Django migration 或 ALTER 扩展）
- 说明：与 `evaluation_result.compliance_task_id` 外键关联；`qb_code` 允许 NULL，便于「先建任务、审核 1 后再写入企标号」的流程。

| 字段名 | MySQL 8（落地） | 类型建议（逻辑） | 说明 |
|---|---|---|---|
| id | BIGINT NOT NULL AUTO_INCREMENT | bigserial | 任务主键 |
| 企标号(qb_code) | VARCHAR(128) DEFAULT NULL，FK → `enterprise_standard_basic.qb_code` | varchar(128) | 确认前可空；确认后关联企标主表 |
