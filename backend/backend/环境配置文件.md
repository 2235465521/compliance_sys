# 本地环境（已在仓库 .gitignore 中忽略，勿提交）
# 其余项请按需从 .env.example 复制或取消注释

DJANGO_SETTINGS_MODULE=config.settings.dev
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=change-me
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# --- Dify 工作流 ①（与 .env.example 示例一致；若主机不同请改 DIFY_API_BASE）---
DIFY_API_BASE=http://192.168.10.225/v1
DIFY_API_KEY=app-ukh7aqCjPNT2XITemDK1luer
# 开始节点「企标文件」变量名（截图：QB_file）
DIFY_WORKFLOW1_FILES_INPUT_KEY=QB_file
# 可选：已发布工作流 UUID → POST /workflows/{id}/run
# DIFY_WORKFLOW_1_ID=
DIFY_WORKFLOW1_OUTPUT_WRAPPER_KEY=QB_init_info

# --- Dify 工作流 ②（国标指标提取，独立应用 Key；开始节点文件变量名一般为 file）---
DIFY_WORKFLOW2_API_KEY=app-NvAlaku02ejMzhg3zaHqXDsH
DIFY_WORKFLOW2_FILES_INPUT_KEY=file
# 与同机 ① 一致时可不改：inputs_single / inputs / files / both
# DIFY_WORKFLOW2_FILE_PAYLOAD_MODE=inputs_single
# DIFY_WORKFLOW_2_ID=
# DIFY_WORKFLOW2_OUTPUT_WRAPPER_KEY=

# --- Dify 工作流 ③（指标对比：enterprise_data + reference_data，独立应用 Key）---
DIFY_WORKFLOW3_API_KEY=app-vzRzgm1sRsLQ9cut9Ib5cc1p
# DIFY_WORKFLOW_3_ID=
# DIFY_WORKFLOW3_OUTPUT_TEXT_KEY=

# --- Dify：批量规范性引用评价（独立应用；与 DIFY_API_KEY 分离；不配则 POST /batch-normative-reference/jobs 返回 503）---
# Base 与同机工作流 ① 一致时可与 DIFY_API_BASE 相同；也可单独指向其它网关
BATCH_NORMATIVE_REF_DIFY_API_BASE=http://192.168.10.225/v1
BATCH_NORMATIVE_REF_DIFY_API_KEY=app-fW39yjRseg5Y4HVy4ngsgCRu
BATCH_NORMATIVE_REF_FILES_INPUT_KEY=QB_file

# --- 主数据库 MySQL（设置 MYSQL_DATABASE 后 default 为 MySQL；reference-latest 等批量查新依赖此库 + v1.0-sql 表）---
# 请在本机创建库并导入 merged_all.sql 等；密码与库名按实际修改后重启 runserver
MYSQL_DATABASE=standards_db_v1.0
MYSQL_USER=root
MYSQL_PASSWORD=root
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306

# 合规模块 / 查新 / 批量规范性引用：强制使用 MySQL（非 MySQL 时相关接口返回 503）
COMPLIANCE_REQUIRE_MYSQL=true
