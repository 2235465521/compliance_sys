from django.apps import AppConfig


class NoveltyConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.novelty"
    label = "novelty"
    verbose_name = "查新服务（8.2）"
