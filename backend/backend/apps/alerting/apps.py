from django.apps import AppConfig


class AlertingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.alerting"
    label = "alerting"
    verbose_name = "预警系统（8.5）"
