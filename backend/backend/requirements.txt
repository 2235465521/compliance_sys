# Web
Django>=5.0,<6.0
django-ninja>=1.3.0
pydantic>=2.7
httpx>=0.27.0

# Data (MySQL) - PyMySQL provides MySQLdb compatibility for Django
pymysql>=1.1.0
# Required by PyMySQL for MySQL 8 caching_sha2_password / sha256_password auth
cryptography>=42.0.0

# Async
celery[redis]>=5.3.0
redis>=5.0.0

# Config
python-dotenv>=1.0.0

# Excel import/export (batch metadata template & .xlsx upload)
openpyxl>=3.1.0

# PDF preview: rewrite Document Info Title so browser tab matches std_code / name
pypdf>=4.0,<6.0
