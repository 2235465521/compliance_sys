# 后端 API 根路径（可按部署环境修改）
# 本地 `npm run dev` 时，vite 已将 /api 代理到 http://127.0.0.1:8000，保持 /api 即可
VITE_API_BASE_URL=/api

<<<<<<< duplicate-check
# 本地开发：仓库内 `frontend/.env.development` 默认把 /api 代理到 192.168.10.218:8000（见 vite.config.ts）
# 端口不对时修改其中 VITE_DEV_PROXY_TARGET 并重启 `npm run dev`；也可用本机 `.env.development.local` 覆盖

# 模板与存档：若后端路由不是 /api/template-archive/... ，在此改前缀（不要末尾斜杠），例如 VITE_TEMPLATE_ARCHIVE_API_PREFIX=/report-center
# VITE_TEMPLATE_ARCHIVE_API_PREFIX=/template-archive
# 无后端时可将下列设为 true，列表接口失败会显示内置示例数据
# VITE_TEMPLATE_ARCHIVE_USE_MOCK=false
=======
# 可选：Dify/Celery 解析完成推送（Channels 房间 dify_notifications）。
# 若不配置且 VITE_API_BASE_URL 为完整 URL（含主机），将尝试推导为 {origin}/ws/dify_notifications/
# VITE_WS_DIFY_URL=ws://127.0.0.1:8000/ws/dify_notifications/
# 后端未开 WebSocket 或路径未对全时，可先关闭推送（页面仍用轮询刷新待审核列表）
# VITE_WS_DIFY_ENABLED=false
>>>>>>> main
