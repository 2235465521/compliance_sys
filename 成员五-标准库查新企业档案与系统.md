# 成员五：标准库、查新、企业档案与系统安全审计开发约定

本文约定**成员五**在并行开发中的职责边界、代码与资源放置位置，以及 Git 合并与自测要求。全组通用规则见同目录 [README.md](./README.md)。

**界面规划（子路由、页面功能与布局）** 详见 [docs/member-five/](./docs/member-five/README.md) 下各模块文档：[标准库](./docs/member-five/01-standard-library.md)、[查新](./docs/member-five/02-novelty-search.md)、[企业档案](./docs/member-five/03-enterprise-archive.md)、[系统安全与审计](./docs/member-five/04-system-security-audit.md)。

---

## 1. 负责范围与路由

成员五负责**四个**业务模块，对应四个独立目录与路由：

| 模块 | 路由 path | 页面入口 |
|------|-----------|----------|
| 标准库管理 | `/standard-library` | `frontend/src/pages/standard-library/index.tsx` |
| 查新服务 | `/novelty-search` | `frontend/src/pages/novelty-search/index.tsx` |
| 企业档案 | `/enterprise-archive` | `frontend/src/pages/enterprise-archive/`（`Layout.tsx` + 子页 `list` / `create` / `detail`） |
| 系统安全与审计 | `/system` | `frontend/src/pages/system/index.tsx` |

**职责概要**：

- **标准库**：行业分类、国标元数据、正文/目录、谱系、查询统计等（大表格、树、表单较多）。
- **查新服务**：任务发起、查新进度与报告入口（与异步任务、PDF 展示可能相关）。
- **企业档案**：企业维度信息与「一企一库」相关管理操作（与需求文档一致）。
- **系统安全与审计**：RBAC、用户角色、审计日志查询（仅超级管理员可见的入口由后端权限 + 前端路由控制配合实现）。

**注意**：四个模块代码量较大，**禁止**在四个目录之间互相 `import` 业务组件；共用部分上收到 `frontend/src/components/`。

---

## 2. 代码目录与文件命名

每个模块使用**独立**的 `pages/<模块名>/` 树，与全组约定一致：

| 共用规则 | 说明 |
|----------|------|
| 入口 | `pages/<模块名>/index.tsx` |
| 子路由 | 若单模块内需多页（如 `/standard-library/metadata`、`/standard-library/lineage`），在 `pages/<模块名>/` 下建子目录，并在 `frontend/src/routes/index.tsx` 中为该模块增加 `children`。**子 path 命名使用 kebab-case**，与现有工程一致。 |
| 组件 / hooks / utils | `pages/<模块名>/components/`、`hooks/`、`utils/` |
| 静态资源 | `pages/<模块名>/assets/` |

**接口、状态、类型**按模块拆分文件（避免单文件过大）：

| 模块 | services | stores | types |
|------|----------|--------|-------|
| 标准库 | `services/standard-library.ts` 或 `services/standard-library/` | `stores/standard-library.ts` | `types/standard-library.ts` |
| 查新 | `services/novelty-search.ts` | `stores/novelty-search.ts`（若需要） | `types/novelty-search.ts` |
| 企业档案 | `services/enterprise-archive.ts` | `stores/enterprise-archive.ts` | `types/enterprise-archive.ts` |
| 系统 | `services/system.ts` | `stores/system.ts` | `types/system.ts` |

当 `services/standard-library.ts` 过大时，拆为 `services/standard-library/metadata.ts`、`lineage.ts` 等，并在目录下 `index.ts` 再导出（与团队评审后执行）。

---

## 3. Ant Design / ProComponents 使用边界

- 标准库、审计日志等**大数据表**优先 **ProTable**，开启分页、虚拟滚动或后端分页，避免一次渲染十万行。
- 谱系、分类等树形数据 **Tree** 或 **ProTable** 树形模式，与后端字段一致。
- 系统模块中 **敏感操作**（删除、导出审计）使用 **Modal.confirm** 二次确认，文案与权限提示与后端错误码对齐。

---

## 4. 接口与 Mock

- 四个模块各自 `services/*.ts`，**不要**合并成单文件。
- 若与成员二（合规）、成员四（预警）共用同一实体（如企业 ID、标准号），在 `types/` 中优先引用**同名字段**或抽一层 `types/common.ts`（需 PR 说明并避免循环依赖）。

---

## 5. Git 分支、PR 与 Code Review

- **个人分支**：在 GitHub 上以主分支为基线创建**自己的功能分支**，所负责四模块相关修改**全部在该分支上**完成后再发 PR 合入主分支；勿直接在主分支上开发。完整流程见 [README.md - GitHub 分支与合并流程](./README.md#github-分支与合并流程)。
- **分支命名**：`feature/<模块简写>-<简述>`，例如 `feature/stdlib-lineage-tree`、`feature/system-audit-log`。
- **PR 粒度**：优先单模块；若必须一次提交多模块（如仅改 `types/common.ts`），在描述中逐条列出。
- **共享文件**：成员五**最可能多次编辑** `routes/index.tsx`（子路由多），合并前务必 **rebase/merge 主分支**，冲突时优先保留他人已合并的路由，再追加自己的 `children`。

---

## 6. 自测清单

- [ ] `npm run build` 通过。
- [ ] 四个 path 均已自测打开：`/standard-library`、`/novelty-search`、`/enterprise-archive`、`/system`。
- [ ] 新增子路由时，侧栏选中高亮与面包屑（若启用）正确。
- [ ] 新增依赖已在 PR 中说明。

---

## 7. 与其他成员的协作点

- **成员二、四**：企业、标准号、任务 ID 等字段在 `types/` 中与对方对齐或抽公共类型。
- **成员一**：仪表盘跳转「标准库/查新」等链接时，路径与本文档一致。

---

## 8. 子路由命名示例（便于扩展时统一）

在 `routes/index.tsx` 中为某一模块增加嵌套路由时，建议子 path 与文件夹一致，例如：

- `standard-library` → `metadata`、`taxonomy`、`lineage`、`fulltext`（示例，以产品为准）
- `system` → `users`、`roles`、`audit-log`

具体名称以产品文档为准，但**保持英文 kebab-case 与文件夹同名**，便于检索与 Code Review。
