# 后端接口说明文档（HTTP / WebSocket）

- 文档生成时间（UTC）：2026-04-18T10:12:48.446204+00:00
- 服务基址示例：`http://127.0.0.1:8000`（业务路径均以 `/api/` 为前缀，除非另行说明）。
- 文末 **curl** 示例中的标准号占位为 `GB/T 30357.2-2013`，仅作演示；联调请换成环境中真实存在的 `bz_id`。

## 1. 全局约定

- **前缀**：业务接口在 **`/api/`** 下；管理后台在 **`/admin/`**。
- **响应形态**：接口可能混用：① DRF 分页 `count` / `next` / `previous` / `results`；② 自定义 `{ "code": 200, "msg", "data" }`；③ `{ "success": true }`；④ **无 `code` 包装**的业务对象（如查新成功）。**请按路径分支解析**。
- **HTTP 状态码**：部分视图在业务失败时仍返回 **HTTP 200**，错误放在 JSON 的 `code` 或 `success` 字段中。
- **跨域**：服务端已配置 `django-cors-headers`；联调时请将前端源加入 `CORS_ALLOWED_ORIGINS`（如使用 credentials）。
- **鉴权**：多数业务接口为 `AllowAny` 或默认无 JWT；**Admin** 需登录 Session。
- **二进制**：`export-report`、`download-doc` 成功时可能返回 Excel/PDF，`Content-Type` 非 JSON。

## 2. HTTP 接口详解（与实现对齐）

### 根与约定

#### 2.1 API 根（DRF 路由列表）

- **方法 / 路径**：`GET` `/api/`
- **请求**

- **Query**：无。
- **Headers**：`Accept: application/json`（可选）。

- **响应**

- **Content-Type**：`application/json`
- **正文**：DRF 默认 API 根，形如 `{ "standards": "http://host/api/standards/", ... }`（各资源入口 URL 字符串）。

---

### 标准基础 StdBaseViewSet（前缀 `/api/standards/`）

#### 2.2 标准分页列表（CRUD 列表）

- **方法 / 路径**：`GET` `/api/standards/`
- **请求**

- **Query（常用）**
  - `page`：页码（与项目 `REST_FRAMEWORK` 分页配置一致）。
  - `page_size`：每页条数（若 `PageNumberPagination` 启用了 `page_size_query_param`；前端标准库列表已传此参数）。
  - `ex_state`：精确筛选执行状态（`DjangoFilterBackend`，字段来自模型）。
  - `search`：`SearchFilter`，在 `bz_id`、`bz_name` 上模糊搜索。
- **Body**：无。

- **响应**

- **成功（HTTP 200）**：DRF 分页 JSON：
```json
{
  "count": 123,
  "next": "http://host/api/standards/?page=2",
  "previous": null,
  "results": [ { "...": "StdBase 模型序列化字段，字段较多，以实际为准" } ]
}
```
- **说明**：`StdBaseSerializer` 使用 `fields = '__all__'`，`results[]` 中单条结构与 `StdBase` 表字段一致。

---

#### 2.3 查新（谱系是否最新）

- **方法 / 路径**：`GET` `/api/standards/check-latest/`
- **请求**

- **Query**
  - `bz_id`（**必填**）：标准号，如 `GB/T 30357.2-2013`。
- **示例**：`/api/standards/check-latest/?bz_id=GB/T%2030357.2-2013`

- **响应**

- **缺参（HTTP 200 + JSON）**
```json
{ "code": 400, "msg": "参数缺失：请提供 bz_id" }
```
- **无谱系（HTTP 200 + JSON）**
```json
{ "code": 404, "msg": "未找到标准 … 的谱系信息" }
```
- **成功（HTTP 200）**：注意**没有**统一的 `code` 包装，直接为业务对象：
```json
{
  "query_bz_id": "GB/T 30357.2-2013",
  "is_latest": true,
  "current_latest_id": "GB/T 30357.2-2013",
  "pedigree_chain": "无链条信息 或 链条文本"
}
```
- **异常**：未捕获异常时可能由框架返回 500 HTML/JSON。

---

#### 2.4 导出合规审查 Excel（单条或批量）

- **方法 / 路径**：`GET` `/api/standards/export-report/`
- **请求**

- **Query**
  - `bz_id`（**必填**）：单个标准号。
- **Body**：无。

- **响应**

- **缺参**
```json
{ "code": 400, "msg": "请提供 bz_id 参数" }
```
- **成功**：`Content-Type` 为 Excel 相关类型（如 `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`），正文为**二进制**（非 JSON）。

---

#### 2.5 导出合规审查 Excel（批量）

- **方法 / 路径**：`POST` `/api/standards/export-report/`
- **请求**

- **Content-Type**：`application/json`
- **Body**
```json
{ "bz_ids": ["GB/T 1.1-2020", "GB/T 1.2-2002"] }
```
- **校验**：`bz_ids` 为空列表时返回 400 JSON。

- **响应**

- **缺参/空列表**
```json
{ "code": 400, "msg": "批量导出列表不能为空" }
```
- **成功**：同 GET，返回 Excel 二进制流。

---

#### 2.6 基础模糊搜索（工作台表格）

- **方法 / 路径**：`GET` `/api/standards/basic-search/`
- **请求**

- **Query**
  - `q`：关键字；**可省略或空字符串**，表示不搜。
- **示例**：`/api/standards/basic-search/?q=GB`

- **响应**

- **`q` 为空**：HTTP 200
```json
{ "code": 200, "msg": "success", "data": [] }
```
- **有结果**：最多约 100 条：
```json
{
  "code": 200,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "bz_id": "GB …",
      "bz_name": "…",
      "ex_state": "现行",
      "release_date": "2020-01-01",
      "implement_time": "2020-07-01"
    }
  ]
}
```
- **服务端异常**：`{ "code": 500, "msg": "服务器内部错误" }`

---

#### 2.7 级联依赖预警追溯

- **方法 / 路径**：`GET` `/api/standards/warning-trace/`
- **请求**

- **Query**
  - `bz_id`（**必填**）：待分析的国标号。

- **响应**

- **缺参**
```json
{ "code": 400, "msg": "请输入需要预警分析的国标号" }
```
- **无谱系 / 链条缺失 / 链条格式异常**：`code` 为 404 或 500，`msg` 说明原因。
- **当前标准已是最新**
```json
{
  "code": 200,
  "msg": "当前标准已是最新",
  "data": { "is_safe": true, "input_bz": "…" }
}
```
- **存在替代且需预警**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "is_safe": false,
    "input_bz": "…",
    "latest_bz": "…",
    "affected_enterprises": ["企标号1", "企标号2"]
  }
}
```

---

#### 2.8 工作台统计

- **方法 / 路径**：`GET` `/api/standards/statistics/`
- **请求**

- **Query / Body**：无。

- **响应**

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "types": { "GB": 0, "QB": 0, "HB": 0, "TB": 0, "DB": 0 },
    "states": { "现行": 0, "废止": 0, "即将实施": 0 }
  }
}
```
- **失败**：`{ "code": 500, "msg": "统计失败: …" }`

---

#### 2.9 批量模糊查新

- **方法 / 路径**：`POST` `/api/standards/batch-fuzzy-check/`
- **请求**

- **Content-Type**：`application/json`
```json
{ "keywords": ["30357", "GB/T 1"] }
```
- **校验**：`keywords` 缺省或非列表、或解析后为空 → 400。

- **响应**

- **空关键字**
```json
{ "code": 400, "msg": "关键字列表为空" }
```
- **成功**
```json
{
  "code": 200,
  "msg": "查询成功",
  "data": [
    {
      "id": "GB/T …",
      "name": "标准名称",
      "isLatest": true,
      "latestId": "GB/T … 或 无替代标准",
      "error": null
    }
  ]
}
```
- **异常**：`code` 500，`msg` 含异常信息。

---

#### 2.10 标准详情卡片（DetailInfo）

- **方法 / 路径**：`GET` `/api/standards/detail-info/`
- **请求**

- **Query**
  - `bz_id`（**必填**）。

- **响应**

- **缺参**：`{ "code": 400, "msg": "缺失标准号" }`
- **无详情记录**：`{ "code": 404, "msg": "暂无该标准的详细信息" }`
- **成功**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "ccs": "…",
    "ics": "…",
    "draft_unit": "…",
    "drafter": "…",
    "report_unit": "…",
    "sub_report_unit": "…"
  }
}
```

---

#### 2.11 下载标准 PDF（本地文档库）

- **方法 / 路径**：`GET` `/api/standards/download-doc/`
- **请求**

- **Query**
  - `bz_id`（**必填**）：原始标准号；服务端会去掉 `/`、空格并转大写，与 `/app/docs` 下 PDF 文件名（同样规则清洗后）做**前缀匹配**。

- **响应**

- **缺参**：`{ "code": 400, "msg": "缺失标准号" }`
- **文档目录不存在**：`{ "code": 500, "msg": "后端找不到文件夹…" }`
- **未匹配到文件**：`{ "code": 404, "msg": "本地文件库中未找到…" }`
- **成功**：`Content-Type: application/pdf`，`Content-Disposition: attachment; filename*=utf-8''…`，正文为 PDF **二进制**。

---

#### 2.12 工作台大屏预警（即将实施 + 近期替代废止提醒）

- **方法 / 路径**：`GET` `/api/standards/dashboard-alerts/`
- **请求**

- **Query / Body**：无。调用时会先根据实施日期把部分「即将实施」自动改为「现行」，并将被替代旧标置为「废止」（写库副作用）。

- **响应**

- **成功**：注意顶层**没有** `msg` 字段，仅有 `code` 与 `data`：
```json
{
  "code": 200,
  "data": {
    "upcoming": [
      {
        "bz_id": "…",
        "bz_name": "…",
        "implement_time": "YYYY-MM-DD",
        "days_left": 7
      }
    ],
    "abolished": [
      {
        "new_bz_id": "…",
        "old_bz_id": "…",
        "message": "【旧】已被新国标【新】替代…",
        "date": "YYYY-MM-DD"
      }
    ]
  }
}
```

---

### 其它 DRF 资源列表（分页）

#### 2.13 detail_info 列表

- **方法 / 路径**：`GET` `/api/detail_info/`
- **请求**

- **Query**：`page`、分页大小（与全局 `REST_FRAMEWORK` 配置一致）；无额外过滤字段（见 `DetailInfoViewSet`）。

- **响应**

- **成功**：`{ "count", "next", "previous", "results" }`；`results[]` 为 `DetailInfo` 全字段序列化。

---

#### 2.14 replaces_table 列表

- **方法 / 路径**：`GET` `/api/replaces_table/`
- **请求**

- **Query**：分页参数。

- **响应**

- **成功**：标准 DRF 分页 JSON；`results[]` 对应 `ReplaceTable`。

---

#### 2.15 indexes_table 列表

- **方法 / 路径**：`GET` `/api/indexes_table/`
- **请求**

- **Query**：分页；`index_type` 精确过滤（`DjangoFilterBackend`）。

- **响应**

- **成功**：标准 DRF 分页 JSON；`results[]` 对应 `IndexTable`。

---

#### 2.16 warnings 列表

- **方法 / 路径**：`GET` `/api/warnings/`
- **请求**

- **Query**：分页。

- **响应**

- **成功**：标准 DRF 分页 JSON；`results[]` 对应 `Warning`（注意与 `/api/warnings/list` 实时预警 JSON 不是同一套数据）。

---

#### 2.17 statistics 资源列表

- **方法 / 路径**：`GET` `/api/statistics/`
- **请求**

- **Query**：分页。

- **响应**

- **成功**：标准 DRF 分页 JSON；`results[]` 对应 `Statistical`（与 `GET /api/standards/statistics/` 工作台统计接口不同）。

---

#### 2.18 relations 列表（标准关系边）

- **方法 / 路径**：`GET` `/api/relations/`
- **请求**

- **Query**：分页。

- **响应**

- **成功**：标准 DRF 分页 JSON；`results[]` 对应 `StandardRelation`。

---

### 族谱图（ECharts）

#### 2.19 获取家族树节点与连线

- **方法 / 路径**：`GET` `/api/get_tree_data/`
- **请求**

- **Query**
  - `bz_id`（**必填**）。

- **响应**

- **缺参**：`{ "code": 400, "msg": "请输入标准号" }`
- **标准不存在**：`{ "code": 404, "msg": "数据库中未找到该标准" }`
- **无 tree_id（孤立节点）**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "nodes": [{ "id": "…", "name": "…", "ex_state": "…" }],
    "links": []
  }
}
```
- **有家族树**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "nodes": [{ "id": "…", "name": "…", "ex_state": "…" }],
    "links": [
      { "source": "父bz", "target": "子bz", "relation_type": "全部代替" }
    ]
  }
}
```
- `relation_type` 在库中为 null/空串时，接口会默认填 `"全部代替"`。

---

### 实时预警（RealtimeWarning）

#### 2.20 巡检并生成实时预警

- **方法 / 路径**：`POST` `/api/warnings/scan`
- **请求**

- **CSRF**：视图使用 `@csrf_exempt`，一般 API 客户端可直接 POST。
- **Body**：可为空；代码未读取 JSON 字段。

- **响应**

- **成功**
```json
{ "success": true, "message": "巡检完成！发现并处理了 N 条冲突！" }
```
- **失败**
```json
{ "success": false, "error": "…" }
```

---

#### 2.21 预警消息列表

- **方法 / 路径**：`GET` `/api/warnings/list`
- **请求**

- **Query / Body**：无。

- **响应**

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "old_bz_id": "…",
      "new_bz_id": "…",
      "quote_bz": "…",
      "is_read": false,
      "create_time": "…"
    }
  ],
  "unread_count": 0
}
```
- **失败**：`success: false`, `error`。

---

#### 2.22 标记单条预警已读

- **方法 / 路径**：`POST` `/api/warnings/mark_read`
- **请求**

- **Content-Type**：`application/json`
```json
{ "warning_id": 1 }
```
- **说明**：`warning_id` 应对 `RealtimeWarning.id`；缺失时可能更新 0 行仍返回 `success: true`。

- **响应**

- **成功**：`{ "success": true }`
- **解析失败等**：`{ "success": false, "error": "…" }`

---

### 审核台与映射（views_dify）

#### 2.23 待审核指标列表

- **方法 / 路径**：`GET` `/api/audit/pending_indexes/`
- **请求**

- **Query / Body**：无。

- **响应**

- **成功**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "bz_id": "…",
      "index_name": "…",
      "index_type": "具体值 | 引用值 | 其他",
      "index_context": { }
    }
  ],
  "total": 1
}
```
- `index_context` 在库中为 JSON 字符串时会解析为对象；解析失败则变为 `{ "error": "JSON解析异常，请打回重提" }`。
- **失败**：`{ "success": false, "error": "…" }`

---

#### 2.24 保存企标 → 现行国标映射（qb_mapping_newgb）

- **方法 / 路径**：`POST` `/api/mapping/save/`
- **请求**

- **Content-Type**：`application/json`
```json
{
  "enterprise_bz_id": "Q/XXX 0001S-2020",
  "national_bz_id": "GB/T 1.1-2020"
}
```
- **校验**：Pydantic `MappingSaveRequest`，字段缺失会返回 `{ "error": "…" }`。

- **响应**

- **校验失败**：`{ "error": "…" }`（非 `success` 包装）
- **重复映射**：`{ "success": false, "error": "该映射关系已存在，无需重复添加" }`
- **成功**：`{ "success": true, "message": "映射保存成功" }`
- **数据库异常**：`{ "success": false, "error": "…" }`

---

#### 2.25 单条审核（通过 / 打回）

- **方法 / 路径**：`POST` `/api/audit/submit/`
- **请求**

- **Content-Type**：`application/json`
- **Body（Pydantic `AuditSubmitRequest`）**
```json
{
  "id": 1,
  "action": "approve",
  "modified_content": { "可选": "修改后的指标 JSON" },
  "modified_bz_id": "可选，修改后的标准号",
  "modified_index_name": "可选，修改后的指标名称"
}
```
- `action`：`"approve"` 更新 `status=1` 并可带修改字段；`"reject"` 置 `status=2`。

- **响应**

- **校验失败**：`{ "error": "…" }`
- **未知 action**：`{ "success": false, "error": "未知的操作类型: …" }`
- **成功**：`{ "success": true, "message": "ID:… 审核操作成功！" }`
- **失败**：`{ "success": false, "error": "…" }`

---

#### 2.26 批量审核（可附带修改后再改状态）

- **方法 / 路径**：`POST` `/api/audit/bulk_submit/`
- **请求**

- **Content-Type**：`application/json`
```json
{
  "ids": [1, 2, 3],
  "action": "approve",
  "edited_records": [
    {
      "id": 1,
      "bz_id": "GB/T …",
      "index_name": "…",
      "index_context": { "k": "v" }
    }
  ]
}
```
- `action`：`approve` → 批量 `status=1`；否则 → `status=2`。
- `edited_records`：可选；若提供则先按条 `UPDATE index_table` 再批量改状态。

- **响应**

- **校验失败**：`{ "error": "…" }`
- **ids 为空**：`{ "success": false, "error": "没有提供任何需要处理的 ID" }`
- **成功**：`{ "success": true, "message": "成功批量处理 N 条数据！" }`

---

### 前言修订差异（StdRevisionDiff + Celery）

#### 2.27 查询前言解析状态 / 结果

- **方法 / 路径**：`GET` `/api/dify/preface-diff/`
- **请求**

- **Query**
  - `bz_id`（**必填**）。

- **响应**

- **缺参**：`{ "code": 400, "msg": "缺失标准号" }`
- **标准不在 StdBase**：HTTP 200
```json
{ "code": 200, "status": "not_found", "msg": "该标准未入库或该标准太老旧" }
```
- **已解析（缓存命中）**：`{ "code": 200, "status": "ready", "data": { …diff_json… } }`（`data` 为模型中的 JSON 字段）
- **已找到 PDF 并已排队解析**：`{ "code": 200, "status": "extracting", "msg": "已定位物理文件，AI 正在深度解析..." }`
- **库中无 PDF 文件**：`{ "code": 200, "status": "file_missing", "msg": "标准文件库缺失 PDF", "bz_id": "…" }`
- **异常**：`{ "code": 500, "msg": "接口异常: …" }`

---

#### 2.28 上传前言 PDF 并触发解析

- **方法 / 路径**：`POST` `/api/dify/preface-upload/`
- **请求**

- **Content-Type**：`multipart/form-data`
- **表单字段**
  - `bz_id`：文本，已存在于 `StdBase` 的标准号。
  - `file`：PDF 文件。

- **响应**

- **缺字段**：`{ "code": 400, "msg": "缺失标准号或文件" }`
- **标准未注册**：`{ "code": 404, "msg": "该标准号尚未在基础信息库注册。" }`
- **成功**：`{ "code": 200, "msg": "文件上传成功并关联到 …！" }`（同时异步任务入队）
- **异常**：`{ "code": 500, "msg": "上传处理失败: …" }`

---

### 企标引用与指标入库

#### 2.29 企标 PDF/DOCX 自动分析（异步 Celery）

- **方法 / 路径**：`POST` `/api/analyze_qb_references_auto/`
- **请求**

- **Content-Type**：`multipart/form-data`
- **表单字段**
  - `file`：**必填**，上传的文件。
- **依赖**：容器内需安装 `redis` Python 包，且 Celery worker 与 broker 可用；否则返回 500 JSON 说明。

- **响应**

- **未上传文件**：`{ "code": 400, "msg": "No file uploaded" }`
- **未安装 redis 包**：`{ "code": 500, "msg": "后端未安装 Python 包 redis…" }`
- **已入队**：`{ "code": 200, "msg": "文件已成功移交后台 AI 引擎…" }`
- **其它异常**：`{ "code": 500, "msg": "任务移交失败: …" }`

---

#### 2.30 规范性引用反向预警链入库

- **方法 / 路径**：`POST` `/api/insert_anti_warn/`
- **请求**

- **Content-Type**：`application/json`（也支持原始 body 外包 Markdown 的 ```json … ``` 代码块，服务端会剥壳）
- **典型 JSON**
```json
{
  "source_bz_id": "Q/XXX 0001S-2020",
  "qibiao_release_date": "2020-01-01",
  "references": [
    { "full_text": "GB/T 1.1-2020", "standard_id": "可选" }
  ]
}
```

- **响应**

- **JSON 解析失败**：`{ "success": false, "error": "JSON解析失败: …" }`
- **成功**：`{ "success": true, "message": "成功将 N 条规范性引用编入映射表…" }`
- **失败**：`{ "success": false, "error": "…" }`

---

#### 2.31 指标批量入库（待审核 status=0）

- **方法 / 路径**：`POST` `/api/insert_indexes/`
- **请求**

- **Content-Type**：`application/json`（或带 ```json … ``` 剥壳的纯文本）
- **Body 形态（灵活）**：服务端对 body 做 JSON 解析后，会递归查找：
  - 标准号：键名可为 `bz_id` / `standard_id` / `标准号`（或在嵌套对象中）。
  - 指标列表：键名可为 `indexes` / `indicators` / `指标列表`；或顶层/嵌套数组，元素含 `index_name`/`name`/`指标名称` 等。
- **单条指标字段（示意）**
```json
{
  "bz_id": "GB/T 1.1-2020",
  "indexes": [
    {
      "index_name": "指标名称",
      "index_type": "具体值",
      "index_context": { "任意": "对象" }
    }
  ]
}
```
- `index_type` 仅接受 `具体值` / `引用值` / `其他`（否则归为 `其他`）。

- **响应**

- **解析失败**：`{ "success": false, "error": "解析失败: …" }`
- **无有效指标**：`{ "success": true, "message": "解析成功，但未发现有效指标数据。" }`
- **成功入库**：`{ "success": true, "message": "成功！标准 [BZ] 的 N 个指标已入库 (status=0)！" }`
- **入库失败**：`{ "success": false, "error": "入库失败: …" }`

---

## 3. 资源级 CRUD（DRF 惯例）

对已注册的 ViewSet，除集合 `GET /api/<资源>/` 外，还支持 REST 惯例，例如：
- `GET /api/standards/{id}/` — 单条详情
- `POST /api/standards/` — 新建
- `PATCH/PUT /api/standards/{id}/` — 更新
- `DELETE /api/standards/{id}/` — 删除

请求体为对应序列化器字段；成功时多为序列化后的对象或 204。请以浏览器 Network 或 DRF 可浏览接口为准。

## 4. WebSocket

- **URL**: `ws://<host>/ws/dify_results/`（HTTPS 部署时用 `wss://`）。
- **消费者**: `DifyResultConsumer`（见 `backend/api/consumers.py`）。
- **服务端推送消息**（`message` 字段内 JSON，常见 `type`）：
  - **`compliance_eval`**：企标引用合规分析完成。载荷含 `source_bz_id`, `qibiao_release_date`, `results`（见 `backend/api/tasks.py` 中 `process_qb_dify_task`）。
  - **`preface_highlights`**：前言差异提取完成。载荷含 `bz_id`, `base_id`, `data`（见 `extract_preface_diff_task`）。
- 前端应在 `onmessage` 中根据 `type` 分支处理。

## 5. 附录：未在 urls 注册的后端函数（当前不可调）

- `POST /api/check_references/` — views_dify.check_references 未在 urls.py 注册
- `POST /api/evaluate_indicators/` — views_dify.evaluate_indicators 未注册
- `POST /api/evaluate_latest_indicators/` — views_dify.evaluate_latest_indicators 未注册

以下为上述函数在源码中的**请求/响应契约摘要**（需在 `urls.py` 注册后方可调用）：

- **POST /api/check_references/（未注册）**（`views_dify.check_references`）
  - 请求：原始 UTF-8 body，可为 ```json 包裹；JSON 内常用 `qibiao_release_date`, `references[]`（含 `standard_id`, `has_year`, `full_text`）。
  - 响应：`{ "success": true, "evaluation_results": [ { "original_text", "status", "message" } ] }` 或 `success: false` / 校验错误。
- **POST /api/evaluate_indicators/（未注册）**（`views_dify.evaluate_indicators`）
  - 请求：`{ "qibiao_bz_id": "企标号" }`（`EvaluateDataRequest`）。
  - 响应：成功：`success`, `enterprise_data`, `reference_data`（均为数组/按引用号分组的指标）；失败：`success: false`, `error`。
- **POST /api/evaluate_latest_indicators/（未注册）**（`views_dify.evaluate_latest_indicators`）
  - 请求：同上 `qibiao_bz_id`。
  - 响应：逻辑使用 `qb_mapping_newgb`；成功字段名仍为 `enterprise_data` / `reference_data`。

## 6. 示例 curl（GET）

```bash
curl -sS "http://127.0.0.1:8000/api/standards/check-latest/?bz_id=GB/T 30357.2-2013"
```

```bash
curl -sS "http://127.0.0.1:8000/api/get_tree_data/?bz_id=GB/T 30357.2-2013"
```
